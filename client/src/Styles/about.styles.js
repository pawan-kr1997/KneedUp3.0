import { Stack as MuiStack, styled } from "@mui/material";

export const AboutContainer = styled(MuiStack)({
    width: "min(47rem, 95vw)",
    margin: "auto",
    marginTop: "7rem",
    textAlign: "center",
});
